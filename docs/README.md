# Project documentation

The frontend and backend run independently during local development:

- Start the FastAPI application from `backend` with `uvicorn app.main:app --reload`.
- Start the Vite application from `Frontend` with `npm run dev`.

See `API.md` for the currently available HTTP endpoints.
