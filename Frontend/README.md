# Container Tracking Frontend

The frontend is a React and TypeScript application built with Vite. It displays the live camera feed, current detection status, and recent detection history.

## Commands

```powershell
npm install
npm run dev
npm run build
npm run lint
```

The application expects the backend at `http://127.0.0.1:8000`.
